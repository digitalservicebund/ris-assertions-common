name: Publish package to GitHub Packages

permissions:
  contents: read

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:

jobs:
  pipeline:
    uses: digitalservicebund/ris-migration-common/.github/workflows/pipeline.yml@e29d672d13a07c98bf9ecfefda1b09d5b836a58a
    secrets: inherit

  publish:
    needs: [pipeline]
    uses: digitalservicebund/ris-migration-common/.github/workflows/publish-package.yml@e29d672d13a07c98bf9ecfefda1b09d5b836a58a
    secrets: inherit
    permissions:
      contents: read
      packages: write
